import { Semesters } from '../../../both/collections/semesters.collection';
import { Semester } from '../../../both/models/semester.model';
/*
Populating the database with the data following the interface
*/
export function loadSemesters() {
	// load the user's semesters from their database attached to their user.
	// Semesters.find({});
	// if (Semesters.find().cursor.count() === 0) {
	//     const semesters: Semester[] = [{
	//     	name: 'Spring 2017'
	//
	//     },
	// 		{
	// 			name: 'Fall 2017'
	// 		},
	// 		{
	// 			name: 'Spring 2018'
	// 		}
	//
	//
	//
	//     ];
	//     semesters.forEach((semester: Semester) => Semesters.insert(semester));
	// }
}