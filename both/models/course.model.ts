//Exporting the interface used for the components of each database object.
export interface Course{
  units: string;
  label: string;
  name: string;
  importance: string;
  status: string;

}